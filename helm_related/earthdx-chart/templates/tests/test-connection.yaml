apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "earthdx-chart.fullname" . }}-test-connection"
  labels:
    {{- include "earthdx-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "earthdx-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
